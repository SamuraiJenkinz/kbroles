---
phase: 05.1-mmc-it-bff-pivot-xmcp-pattern
verified: 2026-04-24T00:22:55Z
status: human_needed
score: "5/5 automated must-haves verified; 2 must-haves require live-deploy human verification"
re_verification: false
human_verification:
  - test: "End-to-end sign-in against live Windows deployment"
    expected: "MMC colleague with KbAssistant.User App Role visits https://<windows-host>, is 302-redirected to Entra, signs in, and lands on role-select with kb_session iron-session HttpOnly cookie."
    why_human: "Requires live Windows + Entra App Registration + pilot user account. Infrastructure (/api/login, /api/auth/callback, AuthProvider, cookie config) verified."
    blocking_SC: "SC#1"
  - test: "Forbidden user flow end-to-end"
    expected: "MMC employee lacking KbAssistant.User signs in, /api/me returns 403, AuthProvider transitions to forbidden, browser lands on /access-denied with role-missing copy."
    why_human: "Requires live deployment + user without role assignment. All supporting logic verified."
    blocking_SC: "SC#2 (live-user arm; stub-driven arm is fully verified)"
  - test: "Live Windows deploy via GitHub Actions self-hosted runner"
    expected: "Push to main triggers workflow. Build on ubuntu-latest. Deploy on [self-hosted, windows, kbassistant]. schtasks stop/rotate/run + /api/health canary + rollback on failure."
    why_human: "Requires physical Windows box + registered self-hosted runner + Scheduled Task + IIS already configured. Workflow verified."
    blocking_SC: "SC#4"
  - test: "IIS reverse-proxy SSE-incremental streaming check"
    expected: "DevTools Network /api/chat Preview shows tokens appearing incrementally. Confirms responseBufferLimit=0 + X-Accel-Buffering:no active."
    why_human: "Requires live IIS+ARR instance. Config documented in docs/deploy-windows.md Step 5.3 + 5.4."
    blocking_SC: "SC#4"
  - test: "Entra setup doc end-to-end execution"
    expected: "Operator completes docs/entra-app-registration-setup.md steps 1-7: create App Registration, secret, KbAssistant.User role, grant Graph User.Read, assign users, write secrets to AWS Secrets Manager, sign in."
    why_human: "Requires Entra tenant access + AWS account access. Doc structure verified."
    blocking_SC: "SC#5"
---

# Phase 5.1: MMC-IT BFF Pivot (xmcp Pattern) Verification Report

**Phase Goal:** Replace Phase 5 SPA+NAA browser auth with the MMC-IT-blessed BFF pattern — server-side Entra auth code flow (@azure/msal-node), iron-session HttpOnly cookie, App Role gating (KbAssistant.User), /api/me BFF contract — and ship a working deploy path to the on-prem Windows Server box (IIS reverse proxy + Windows Scheduled Task + AWS Secrets Manager) with a user-executable Entra App Registration setup doc as the handoff artifact.

**Verified:** 2026-04-24T00:22:55Z

**Status:** human_needed — all automated checks pass; live-deployment arms of SC#1, SC#2, SC#4, SC#5 require human execution against the Windows box + Entra tenant.

**Re-verification:** No (initial verification).

## Goal Achievement

### Observable Truths

| #   | Truth (from ROADMAP SC) | Status | Evidence |
| --- | --- | --- | --- |
| 1a  | Sign-in infrastructure exists | VERIFIED | /api/login 302s to Entra via cca.getAuthCodeUrl; /api/auth/callback exchanges code + saveSession; AuthProvider fetches /api/me; session.ts sets httpOnly+secure(prod)+sameSite:lax+maxAge=8h |
| 1b  | Live Windows sign-in end-to-end | HUMAN | Requires live Windows + Entra + pilot user |
| 2a  | /api/me 403 with upn; AuthProvider routes forbidden to /access-denied with role-missing copy | VERIFIED | me/route.ts:47-52 emit 403 {error:forbidden, upn}; AuthProvider.tsx:57-66 transition with upn; ChatPage.tsx:35-37 router.replace(/access-denied); access-denied page copy role-missing NOT tenant-wrong |
| 2b  | Live forbidden-user flow | HUMAN | Unit tests cover every arm; E2E stubBffForbidden fixture present |
| 3   | /api/chat wire codes: 401 token_expired / 403 access_denied / 401 unauthorized | VERIFIED | chat/route.ts:171-189 map each discriminant; chat/__tests__/route.test.ts:697/724/744 assert each; ErrorCard.tsx:33 labels token_expired Sign back in; ChatSurface.tsx:216 window.location.href=/api/login |
| 4a  | Deploy workflow targets self-hosted Windows + schtasks + rollback + canary | VERIFIED | deploy.yml:66 runs-on [self-hosted, windows, kbassistant]; schtasks used 6x; 0 NSSM; rollback 122-143; canary Invoke-RestMethod /api/health 115-120 |
| 4b  | Actual live deploy | HUMAN | Cannot execute without Windows runner |
| 5a  | entra-app-registration-setup.md complete and executable | VERIFIED | 7 steps + AADSTS50011/7000218/ERR_SSL troubleshooting + aws secretsmanager create-secret CLI + /mmc/cts/kb-assistant secret id + reference table (203 lines) |
| 5b  | Operator completes Entra setup | HUMAN | Requires Entra tenant + AWS account access |

**Automated Score:** 5/5 verifiable infrastructure truths in place.

**Human verification needed:** SC#1 live sign-in, SC#2 live-user forbidden, SC#4 actual deploy + IIS-SSE, SC#5 Entra portal execution.

### Required Artifacts

| Artifact | Status | Details |
| --- | --- | --- |
| src/auth/msalClient.ts | VERIFIED | 65 lines; lazy getCca(); msalClient.test.ts 5 tests pass |
| src/auth/session.ts | VERIFIED | 94 lines; SESSION_COOKIE_NAME=kb_session; httpOnly/secure:prod/lax/maxAge=8h; getSession/saveSession/clearSession; 6 tests pass |
| src/app/api/login/route.ts | VERIFIED | 42 lines; runtime=nodejs; cca.getAuthCodeUrl; 3 tests pass |
| src/app/api/auth/callback/route.ts | VERIFIED | 91 lines; runtime=nodejs; acquireTokenByCode + Pitfall 5 roles coercion + saveSession; 5 tests pass |
| src/app/api/logout/route.ts | VERIFIED | 26 lines; 1 test passes |
| src/app/api/me/route.ts | VERIFIED | 63 lines; 401/403/200 shapes; Cache-Control:no-store; 5 tests pass |
| src/app/api/_middleware.ts | VERIFIED | 86 lines; 4-way discriminant; dev stub preserved; 7 tests pass |
| src/app/access-denied/page.tsx | VERIFIED | 48 lines; role-missing copy; 5 tests pass |
| src/chat-ui/AuthProvider.tsx | VERIFIED | 91 lines; fetch /api/me credentials:include; full state machine |
| src/chat-ui/ChatPage.tsx | VERIFIED | unauth to window.location.href /api/login; forbidden to router.replace /access-denied |
| src/chat-ui/useChatStream.ts | VERIFIED | credentials:include; token_expired dispatch; 0 Bearer |
| src/chat-ui/ErrorCard + ChatSurface retry | VERIFIED | Sign back in label; window.location.href=/api/login on retry |
| docs/entra-app-registration-setup.md | VERIFIED | 203 lines; 7 steps; AADSTS troubleshooting; CLI |
| docs/deploy-windows.md | VERIFIED | 339 lines; Scheduled Task + IIS+ARR + responseBufferLimit=0 + X-Accel-Buffering:no + 127.0.0.1:3001; no NSSM |
| .github/workflows/deploy.yml | VERIFIED | 144 lines; self-hosted labels; schtasks 6 refs; canary; rollback |
| tests-e2e/fixtures/mockSession.ts | VERIFIED | stubBffAuthenticated/Forbidden; 11 specs consume |
| tests-e2e/auth-middleware-from-azure.spec.ts | VERIFIED | APP_HOSTNAME env; test.skip when unset |

### Key Link Verification

| From | To | Via | Status |
| --- | --- | --- | --- |
| providers.tsx | AuthProvider | import + wrap | WIRED |
| AuthProvider | /api/me | fetch credentials:include | WIRED |
| ChatPage | AuthProvider | useAuth() | WIRED |
| ChatPage | /access-denied | router.replace | WIRED |
| ChatPage | /api/login | window.location.href | WIRED |
| useChatStream | /api/chat | fetch credentials:include | WIRED |
| /api/login | msal-node | getCca().getAuthCodeUrl | WIRED |
| /api/auth/callback | saveSession | iron-session | WIRED |
| /api/me | getSession | iron-session cookieStore | WIRED |
| /api/chat | _middleware.getRequestUser | discriminant to wire codes | WIRED |
| deploy.yml | schtasks | pwsh | WIRED |
| deploy.yml | /api/health | Invoke-RestMethod canary | WIRED |
| env config | AWS Secrets Manager | @aws-sdk/client-secrets-manager | WIRED |

### Requirements Coverage

Phase 5.1 addresses AUTH-01, DELV-01 (on-prem Windows), DELV-02, DELV-04. AUTH-03 + DELV-03 deferred to v1.1.

| Requirement | Status | Notes |
| --- | --- | --- |
| AUTH-01 Entra SSO | SATISFIED (infra); HUMAN (live) | Auth code flow complete |
| DELV-01 On-prem Windows deploy | SATISFIED (workflow+docs); HUMAN (actual) | Workflow + docs complete |
| DELV-02 Deployment automation | SATISFIED | Build on ubuntu; deploy self-hosted; canary + rollback |
| DELV-04 AWS Secrets Manager | SATISFIED (infra); HUMAN (secret create) | SDK wiring in place |
| AUTH-03 Teams SSO | DEFERRED | v1.1 |
| DELV-03 Teams manifest | DEFERRED | v1.1 |

### Anti-Patterns Found

| Search | Expected | Actual | Verdict |
| --- | --- | --- | --- |
| @azure/msal-browser in code | 0 | 0 | clean |
| @azure/msal-react in code | 0 | 0 | clean |
| @microsoft/teams-js in code | 0 | 0 | clean |
| mock-jwks in code | 0 | 0 | clean |
| jose in src code | 0 | 0 (doc-comment refs only) | clean |
| NEXT_PUBLIC_ENTRA in src + next.config.ts | 0 | 0 | clean (Pitfall 9) |
| teams-js/MsalProvider/PublicClientApplication | 0 | 2 files w/ historical doc-comment refs only | clean |
| NSSM/nssm in code/workflow | 0 | 0 (6 refs all in .planning/ docs) | clean |
| Bearer/Authorization in chat-ui | 0 | 2 comment matches only | clean |
| @azure/msal-node in package.json | 1 | 1 (^5.1.4) | present |
| iron-session in package.json | 1 | 1 (^8.0.4) | present |
| @aws-sdk/client-secrets-manager | 1 | 1 (^3.1036.0) | present |
| KbAssistant.User in src | >=2 | 2 constants + fixtures/copy | ok |
| schtasks in deploy.yml | >=3 | 6 | ok |
| credentials:include in chat-ui | >=1 | 4 real use sites | ok |
| fetch /api/me in AuthProvider | 1 | 1 | ok |
| stubBffAuthenticated in tests-e2e | >=11 specs | 11 specs + fixture = 12 | ok |
| stubBffForbidden fixture | >=1 | 1 | ok |

No blocker anti-patterns. Doc-comment references to replaced Phase-5 terminology are historical documentation, not active code.

### Automated Tests

| Suite | Command | Result |
| --- | --- | --- |
| TypeScript typecheck | pnpm typecheck (tsc --noEmit) | exit 0, no errors |
| Unit tests (Vitest) | pnpm test --run | 597 tests pass across 60 files (matches baseline) |
| Playwright E2E | pnpm exec playwright test | 19 passed + 2 skipped (remote smokes skip cleanly when APP_HOSTNAME unset) |

### Human Verification Required

Five items require live-deploy human validation (structured form in YAML frontmatter):

1. **Live Entra sign-in happy path (SC#1):** Requires Windows host + Entra App Registration + pilot user. Infrastructure verified.
2. **Live forbidden-user flow (SC#2 live arm):** Requires MMC account lacking KbAssistant.User role. Unit + stub-driven E2E arms verified.
3. **Actual Windows deploy (SC#4 execution):** Requires self-hosted runner + Windows box. Workflow + pwsh scripts verified.
4. **IIS SSE-incremental streaming (SC#4 streaming arm):** Requires live IIS+ARR. Config verified in docs.
5. **Entra portal + AWS Secrets Manager execution (SC#5 execution):** Requires Entra + AWS tenant access. Doc completeness verified.

None of these are code gaps. All are live-deployment validations that the code + docs have set up correctly.

### Gaps Summary

**No code gaps.** Everything verifiable against the repository without live Windows/Entra access has been verified and passes. The phase is code-complete and ready for human handoff to the on-prem Windows deployment + Entra App Registration setup.

The distinction between passed and human_needed here is deliberate: SC#1 through SC#5 all have significant arms that cannot be executed without the target runtime environment. Once a human operator completes the five live-deploy tests and confirms pass, this verification can be upgraded to passed via re-verification.

---

_Verified: 2026-04-24T00:22:55Z_
_Verifier: Claude (gsd-verifier)_
