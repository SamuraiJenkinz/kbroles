---
phase: 05.1-mmc-it-bff-pivot-xmcp-pattern
plan: 08
subsystem: docs
tags: [operator-docs, entra, windows-server, iis, aws-secrets-manager, scheduled-task, self-hosted-runner, roadmap, pitfall-4, pitfall-5, pitfall-6]

# Dependency graph
requires:
  - phase: 05.1-01
    provides: loadSecrets() module + AWS_* env fields (referenced by env-handling.md §5)
  - phase: 05.1-02
    provides: msalClient.ts + session.ts (referenced by entra-app-registration-setup.md Step 3 REQUIRED_ROLE cross-ref)
  - phase: 05.1-03
    provides: /api/login + /api/auth/callback + /api/logout + /api/me (referenced by entra-app-registration-setup.md redirect URI + smoke test)
  - phase: 05.1-06
    provides: Clean tree — no NEXT_PUBLIC_ENTRA_* references (consumed by env-handling.md §5 wording "no NEXT_PUBLIC_* vars in use as of Phase 5.1")
  - phase: 05.1-07
    provides: .github/workflows/deploy.yml (referenced throughout deploy-windows.md as the contract the runbook supports)
provides:
  - docs/entra-app-registration-setup.md (203-line operator runbook — 7 steps + troubleshooting + rotation)
  - docs/deploy-windows.md (339-line operator runbook — 7 steps + rollback + file/path inventory)
  - docs/env-handling.md §5 rewritten (Phase-5 Azure App Service plan replaced with Phase 5.1 AWS Secrets Manager + loadSecrets() cascade)
  - ROADMAP.md Phase 5.1 block (goal, 5 success criteria, 8-plan checklist all [x] complete, Pitfall focus, AUTH-03/DELV-03 deferred-to-v1.1 annotations)
  - ROADMAP.md pivot note (2026-04-23) explaining Phase 5 → Phase 5.1 supersession
affects: [phase-6-observability, phase-6-pilot-rollout, v1.1-teams-tab-candidate]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Operator runbook structure: step-by-step click/type instructions → 'How you know it worked' verification block after each step → explicit troubleshooting matrix for AADSTS50011 / 7000218 / 403 / IIS 502/500 / SSE buffering"
    - "AWS CLI as primary path over Console for scriptable/auditable operator tasks (aws secretsmanager create-secret / update-secret / get-secret-value)"
    - "Reference tables at end of each runbook mapping operator-entered values → env keys → consuming source modules"

key-files:
  created:
    - docs/entra-app-registration-setup.md
    - docs/deploy-windows.md
    - .planning/phases/05.1-mmc-it-bff-pivot-xmcp-pattern/05.1-08-SUMMARY.md
  modified:
    - docs/env-handling.md
    - .planning/ROADMAP.md

key-decisions:
  - "ROADMAP Progress table reflects reality faithfully: 5.1 MMC-IT BFF pivot shows 8/8 Complete 2026-04-23 (NOT the 0/8 Not started placeholder from plan-write time). All 8 plan items marked [x] with completion date annotations."
  - "Coverage Validation table left entirely unchanged — AUTH-03 + DELV-03 remain in the Phase 5 row (6 requirements). The deferral is documented via the annotation on the Phase 5 Requirements line, not by re-mapping. Avoids orphaning the coverage count (49/49 unchanged)."
  - "Full scrub of Azure references in env-handling.md beyond the §5 replacement — status line, per-runtime cheat sheet row for next start, NODE_EXTRA_CA_CERTS production guidance in §3, MSAL secret entry in §3, .env.example inline comment on LLM_API_KEY, appendix closing sentence. No Azure / App Service / Key Vault text remains anywhere in the doc."
  - "NSSM mention scrubbed from deploy-windows.md even in the 'NOT NSSM' disambiguation sentence. Orchestrator success criterion was NSSM|nssm grep = 0 matches; replaced disambiguation to 'Not a Windows Service wrapper, not a user-mode process manager' to preserve the semantic teaching without tripping the grep."
  - "AWS CLI as primary path over AWS Console in entra-app-registration-setup.md Step 6 (aligns with planning-context principle #3: don't instruct user to do work that can be scripted; Entra portal is human-only because Microsoft lacks CLI for App Role creation, but AWS Secrets Manager has full CLI parity)."

patterns-established:
  - "Pitfall-inline pattern: each operator step that corresponds to a documented Pitfall references it inline (e.g. 'Redirect URI exact match — Pitfall 4', 'responseBufferLimit=0 — Pitfall 6', 'roles claim absent → [] coerce — Pitfall 5'). Operators encountering the symptom can grep RESEARCH.md for the architectural rationale without hunting."
  - "Three-tier documentation hierarchy: RESEARCH.md (architecture rationale, engineer-facing) → PLAN.md (task execution, executor-facing) → docs/*.md (operator-facing click/type instructions). No content duplication between tiers — each reader type has the exactly-right artifact for their role."
  - "Deferred-to-v1.1 annotation style in ROADMAP.md: Requirements line inline annotation (e.g. 'AUTH-03 (deferred to v1.1)') + paragraph-level pivot note linking to RESEARCH + explicit 'Requirements addressed / Requirements deferred to v1.1' split in the new phase's description. Preserves coverage mapping (49/49) without re-assigning requirements across phases."

# Metrics
duration: ~12 min
completed: 2026-04-23
---

# Phase 5.1 Plan 08: Operator Docs + Entra + Windows + Roadmap Summary

**Three operator runbooks (Entra App Registration 203-line, Windows Server deploy 339-line, env-handling.md §5 AWS Secrets Manager rewrite) + ROADMAP Phase 5.1 expansion (goal, 5 success criteria, 8-plan [x]-complete checklist, Pitfall focus, AUTH-03/DELV-03 deferred-to-v1.1 annotations, pivot note) — the docs capstone that makes Phase 5.1 ready-to-ship.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-04-23T19:05:00Z (approx — matches Task 1 commit window)
- **Completed:** 2026-04-23T19:19:07Z (last pnpm test run)
- **Tasks:** 3 (all `type="auto"`, no checkpoints)
- **Files created:** 2 (docs/entra-app-registration-setup.md, docs/deploy-windows.md) + this SUMMARY.md
- **Files modified:** 2 (docs/env-handling.md, .planning/ROADMAP.md)
- **Commits:** 3 atomic task commits + 1 docs-completion commit (this SUMMARY)

## Accomplishments

- **docs/entra-app-registration-setup.md created** (203 lines) — self-contained operator runbook for the one-time Entra portal setup. Seven steps: (1) Create App Registration (Web platform, redirect URI with Pitfall 4 callout), (2) Create client secret, (3) Define App Role `KbAssistant.User` with Pitfall 5 roles-claim-absent explanation, (4) Grant API permissions + admin consent, (5) Assign pilot users/group via Enterprise Applications, (6) Write secrets to AWS Secrets Manager (AWS CLI primary path, Console fallback), (7) Smoke test sign-in flow. Troubleshooting matrix for AADSTS50011 / 7000218 / 403. Rotation section (client-secret 60-day reminder, pilot user additions, v1.1 multi-host prep). Closing reference table mapping Entra values → AWS Secrets Manager keys → consuming source modules (msalClient.ts, session.ts, _middleware.ts).
- **docs/deploy-windows.md created** (339 lines) — self-contained operator runbook for the on-prem Windows Server box. Seven steps: (1) Install Node.js 20 LTS system-wide, (2) Verify AWS credentials via `aws secretsmanager get-secret-value` under the Scheduled Task's user account, (3) Create deploy dir `D:\kbroles\.next\standalone` + `D:\logs`, (4) Create Windows Scheduled Task `KbAssistant` (three subsections: GUI config, machine-scope env vars, optional PowerShell log-wrapper), (5) Configure IIS reverse proxy (URL Rewrite + ARR + Pitfall 6 `responseBufferLimit=0` + `X-Accel-Buffering:no` header + `X-Forwarded-Proto: https` server variable + allowedServerVariables one-time config), (6) Register GitHub Actions self-hosted runner with `kbassistant` label + optional Repository Variables (`KBASSISTANT_DEPLOY_ROOT` / `_SCHEDULED_TASK` / `_HEALTH_URL`), (7) First deploy + verify. Rollback section (auto-rollback + manual rollback procedure). Reference file/path inventory table. Troubleshooting matrix for Node crash / Scheduled Task failure / IIS 502 / IIS 500 / SSE buffering. Zero NSSM mentions — Scheduled Task referenced throughout with `schtasks` command examples (20 occurrences).
- **docs/env-handling.md §5 rewritten** — the old "App Service Application Settings Mapping" section (App Setting keys → Azure Key Vault references → bicep template) replaced with a Phase 5.1 "AWS Secrets Manager + loadSecrets() cascade" section. Covers: the three-step runtime cascade (AWS Secrets Manager JSON blob → `loadSecrets()` with dev-wins/module-cached semantics → `env()` zod validation), local dev `.env.local` example including the new `SESSION_SECRET` + `ENTRA_CLIENT_SECRET` + `APP_BASE_URL` keys, production env var sourcing on the Windows Scheduled Task, secret rotation procedure, and a runtime env-key inventory table. Surgical scrub of all other Azure references preserved intact (status line, per-runtime cheat sheet `next start` row, `NODE_EXTRA_CA_CERTS` §3 production entry, MSAL secret §3 entry, `.env.example` inline comment on LLM_API_KEY, troubleshooting-appendix closing sentence). Sections 1 (Files & Load Order), 2 (Per-Runtime Cheat Sheet header + dev/vitest/smoke rows), 4 (.env.example canonical list), 6 (Troubleshooting), 7 (Appendix) left substantively unchanged — only the Azure-specific prose was surgically replaced.
- **.planning/ROADMAP.md Phase 5.1 block expanded** — `Goal: [Urgent work - to be planned]` + `Plans: 0 plans` + TBD placeholder REPLACED with: the real phase goal (server-side Entra + iron-session + App Role gating + on-prem Windows deploy with operator runbook handoff), "Depends on" breadcrumb to Phase 5, explicit "Requirements addressed" / "Requirements deferred to v1.1" split (AUTH-01 DELV-01 DELV-02 DELV-04 addressed; AUTH-03 DELV-03 deferred), five Success Criteria covering sign-in happy path / access-denied path / error-code wire contract / deploy infrastructure / operator-runbook completion, Pitfall focus list (1, 3, 4, 5, 6, 8, 9, 10, 11), full 8-plan checklist with every plan marked `[x]` + `(complete 2026-04-23)` annotation.
- **.planning/ROADMAP.md Phase 5 Requirements line annotated** — `AUTH-01, AUTH-03, DELV-01, DELV-02, DELV-03, DELV-04` replaced with `AUTH-01, AUTH-03 (deferred to v1.1), DELV-01 (reassigned to on-prem Windows — Phase 5.1), DELV-02, DELV-03 (deferred to v1.1), DELV-04`.
- **.planning/ROADMAP.md pivot note (2026-04-23)** added immediately above the Phase 5.1 header explaining: Phase 5 is superseded; Phase 5.1 replaces it with BFF pattern; AUTH-03 + DELV-03 deferred to v1.1; a Phase 6.1 Teams-tab candidate exists in RESEARCH but is NOT on the v1 roadmap (separate user decision — aligns with orchestrator's "DO NOT add a Phase 6.1: Teams tab entry without user confirmation" constraint).
- **.planning/ROADMAP.md Progress table updated** — `| 5.1 MMC-IT BFF pivot (xmcp pattern) | 0/8 | Not started | - |` replaced with `| 5.1 MMC-IT BFF pivot | 8/8 | Complete | 2026-04-23 |`. (Phase 5 row `4/5 | Paused (pivoted to 5.1) | 2026-04-23` left intact — was already correct.)
- **597/597 unit tests still green**, typecheck clean. This plan touches only markdown files so the baseline was never at risk, but verified end-to-end to honour the plan's verification block.

## Task Commits

Each task was committed atomically with only the files listed in that task's `<files>` element staged (verified via `git diff --name-only --cached` before each commit):

1. **Task 1: Write docs/entra-app-registration-setup.md** — `4d7de2a` (docs)
2. **Task 2: Write docs/deploy-windows.md** — `651a8d7` (docs)
3. **Task 3: Update docs/env-handling.md + .planning/ROADMAP.md** — `c7dff73` (docs)

Plan docs commit (this SUMMARY) follows.

## Files Created/Modified

### Created

- `docs/entra-app-registration-setup.md` — 203 lines. Seven-step operator runbook for KB Assistant Entra App Registration. Covers platform=Web + redirect URI (Pitfall 4 inline), client secret generation, App Role `KbAssistant.User` definition (Pitfall 5 inline), admin-consented API permissions, Enterprise-Applications user/group assignment, AWS Secrets Manager seed via CLI primary path, sign-in smoke test. Troubleshooting for AADSTS50011 / 7000218 / 403 / SSL cert. Rotation section. Reference table at end.
- `docs/deploy-windows.md` — 339 lines. Seven-step operator runbook for the Windows Server box. Covers Node 20 install system-wide, AWS credentials verification under the Scheduled Task's account, deploy dir + log dir creation, Windows Scheduled Task `KbAssistant` full configuration (GUI walkthrough + machine-scope env vars + optional PowerShell logging wrapper), IIS reverse proxy (URL Rewrite + ARR + Pitfall 6 SSE-safe settings), self-hosted runner registration with `kbassistant` label, first deploy + verify, rollback procedure. Reference file/path inventory table at end. Troubleshooting matrix at end of Step 7.
- `.planning/phases/05.1-mmc-it-bff-pivot-xmcp-pattern/05.1-08-SUMMARY.md` — this file.

### Modified

- `docs/env-handling.md` — 182 → 217 lines. §5 (formerly "App Service Application Settings Mapping") rewritten in full as "Phase 5.1 — AWS Secrets Manager + loadSecrets() cascade". Surgical Azure-reference scrubs across status line, runtime cheat sheet `next start` row, `NODE_EXTRA_CA_CERTS` production guidance in §3, MSAL secret entry in §3, `.env.example` inline comment on LLM_API_KEY, appendix closing sentence. Sections 1, 2 (except row 2), 4, 6, 7 left substantively unchanged — the scrub touched one Azure-referencing sentence or table cell per section at most.
- `.planning/ROADMAP.md` — 248 → 262 lines. Three surgical changes: (1) Phase 5 Requirements annotation, (2) pivot note inserted above Phase 5.1 header, (3) Phase 5.1 body fully expanded replacing the plan-write-time TBD placeholder, (4) Progress table row corrected to 8/8 Complete. Coverage Validation section + other phase sections untouched.

## env-handling.md content-preservation inventory

The plan said "Preserve all OTHER sections unchanged" aside from the Azure→AWS replacement. Actual preservation scope:

- **§1 Files & Load Order** — table and prose completely unchanged. All four file rows + Next.js precedence paragraph + Tsx/plain-Node paragraph preserved verbatim.
- **§2 Per-Runtime Cheat Sheet** — header table column renamed `App Service source` → `Production source`. The `next start` row updated to describe the Windows Scheduled Task + loadSecrets() pattern (was Azure App Service Application Settings). Other three rows (next dev / vitest run / pnpm smoke) and the three explanatory paragraphs below the table unchanged.
- **§3 Secrets That MUST Live Outside .env Files** — intro sentence updated (App Service → Windows Server machine-scope). `NODE_EXTRA_CA_CERTS` entry: purpose/why-not-.env/local-dev/Windows-dev/Verification preserved; the Azure-specific production bullet rewritten to point to Windows machine-scope env var + `docs/deploy-windows.md` Step 4.2. MSAL secret entry rewritten end-to-end (Azure Key Vault → AWS Secrets Manager) per plan.
- **§4 .env.example** — canonical variable list unchanged except for the one-line inline comment above `LLM_API_KEY` (was "App Service Application Settings in prod"; now "AWS Secrets Manager → loadSecrets() in prod"). All other comments + schema + shell-only note preserved verbatim.
- **§5 (replaced)** — full rewrite, plan-directed. ~35 lines of Phase 5.1 content replaces ~17 lines of Phase 5 Azure content.
- **§6 Troubleshooting** — all four error scenarios (zod validation / UNABLE_TO_VERIFY_LEAF_SIGNATURE / process.env undefined in Vitest / turbopack pino / tsx env) preserved verbatim.
- **§7 Appendix — Why This Matters** — three bullet points preserved; closing sentence updated from "CI / App Service" → "CI / on the Windows box".

Net: no Phase-1 or Phase-2 architectural content lost, no Phase-5.1 content omitted, Azure references scrubbed completely.

## Coverage Validation table — adjustments required?

**None.** Plan said the Coverage Validation table should remain unchanged because AUTH-03 + DELV-03 are being *deferred* (still exist, moved in time) not *removed*. Verified post-edit: the table still shows `5 | 6 | AUTH-01, AUTH-03, DELV-01, DELV-02, DELV-03, DELV-04` with the total `49 / 49 ✓` mapped. The deferral is communicated via the annotation on the Phase 5 Requirements line (step a of Task 3) — not by re-mapping requirements across phases.

## Decisions Made

1. **ROADMAP Progress table reflects reality faithfully.** The orchestrator prompt called out that the PLAN's Task 3c step included the placeholder-era `0/8 Not started` text, but by the time this SUMMARY commit lands, all 8 plans (including this one) are complete. Wrote `| 5.1 MMC-IT BFF pivot | 8/8 | Complete | 2026-04-23 |` to match reality. All 8 plan checklist items are `[x]` with `(complete 2026-04-23)` annotations. Orchestrator's phase-completion step will also verify this.

2. **Coverage Validation table unchanged.** AUTH-03 + DELV-03 remain mapped to Phase 5 (6 requirements, matching the phase's Requirements-line count). The deferral is documented via the inline annotation `(deferred to v1.1)` on the Requirements line and in the Phase 5.1 body's explicit "Requirements deferred to v1.1" split. Re-mapping the two requirements to a hypothetical future phase would orphan the coverage count or force inventing a Phase 6.1 row, both of which the orchestrator prompt forbids.

3. **Full Azure scrub in env-handling.md, not just the §5 replacement.** The plan's `<action>` text said "replace any existing section about Azure App Service env vars" — implying a single-section surgical edit. The orchestrator's success criterion was stricter: `Grep 'Azure App Service' in docs/env-handling.md returns NO matches`. Scrubbed all residual references across 7 locations (status line, per-runtime cheat sheet row, §3 intro + NODE_EXTRA_CA_CERTS + MSAL entries, .env.example comment, appendix closing). Every scrub preserved the surrounding semantic content by substituting the equivalent Phase 5.1 pattern (machine-scope env vars + AWS Secrets Manager + Windows Scheduled Task) — no content lost, just the hosting-target narrative swapped.

4. **NSSM scrubbed even from the "NOT NSSM" disambiguation sentence.** Initial draft of deploy-windows.md Step 4 opened with "runs as a Windows Scheduled Task (NOT NSSM, NOT a Windows Service wrapper, NOT PM2)" — semantically accurate but the token `NSSM` itself tripped the orchestrator's grep = 0 gate. Rewrote to "Windows Scheduled Task — this is the MMC standard per the planning context. Not a Windows Service wrapper, not a user-mode process manager." Preserves the disambiguation teaching without the banned token.

5. **AWS CLI as primary path in entra-app-registration-setup.md Step 6.** The planning-context principle #3 said "don't instruct user to do work that can be scripted". AWS Secrets Manager has full CLI parity (`create-secret`, `update-secret`, `get-secret-value`); the Entra portal App Role creation does not. Step 6 leads with a PowerShell `aws secretsmanager create-secret` example (with `update-secret` variant for rotation re-runs), mentioning the AWS Console as a fallback for operators who prefer a GUI. All other Entra-portal steps remain human-walkthrough because Microsoft has no equivalent CLI.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking] Scrubbed "NSSM" from the "NOT NSSM" disambiguation in deploy-windows.md**

- **Found during:** Task 2 (verification — `Grep NSSM|nssm docs/deploy-windows.md` returned 1 match on the intentional "NOT NSSM" sentence).
- **Issue:** Orchestrator success criterion `Grep 'NSSM|nssm' across docs/ + .planning/ROADMAP.md returns 0 matches` was strictly zero-tolerance. My initial Step 4 opening sentence "runs as a Windows **Scheduled Task** (NOT NSSM, NOT a Windows Service wrapper, NOT PM2)" was semantically correct disambiguation but would trip the grep.
- **Fix:** Rewrote the sentence to "Windows **Scheduled Task** — this is the MMC standard per the planning context. Not a Windows Service wrapper, not a user-mode process manager." Preserves the teaching (operator understands supervisor choice and why) without the banned token.
- **Files modified:** `docs/deploy-windows.md` (pre-commit)
- **Verification:** `Grep NSSM|nssm docs/deploy-windows.md` returned 0 matches post-edit.
- **Committed in:** `651a8d7` (Task 2 commit).

**2. [Rule 3 — Blocking] Extended Azure-reference scrub in env-handling.md beyond the §5 section**

- **Found during:** Task 3 (verification — `Grep 'Azure App Service' docs/env-handling.md` returned 1 match, `Grep 'App Service' docs/env-handling.md` returned 5 matches).
- **Issue:** Plan's `<action>` wording ("replace any existing section about Azure App Service env vars") implied a single-section edit, but the orchestrator's `<verify>` block explicitly required `Grep 'Azure App Service' returns NO matches (post-replace)`. Six residual Azure references remained across six sections after my first edit.
- **Fix:** Surgical edits to the status line, per-runtime cheat sheet `next start` row (column rename + row content), §3 intro sentence, `NODE_EXTRA_CA_CERTS` production bullet, MSAL secret entry header, `.env.example` LLM_API_KEY inline comment, and §7 appendix closing sentence. Each edit substituted the equivalent Phase 5.1 pattern (Windows Scheduled Task + machine-scope env + AWS Secrets Manager). No content lost — hosting-target narrative swapped in-place.
- **Files modified:** `docs/env-handling.md` (pre-commit)
- **Verification:** `Grep 'Azure App Service|Azure Key Vault|\bApp Service\b' docs/env-handling.md` returned 0 matches post-edit; `Grep 'Azure' docs/env-handling.md` returned 0 matches; `Grep 'AWS Secrets Manager|loadSecrets' docs/env-handling.md` returned 19 matches.
- **Committed in:** `c7dff73` (Task 3 commit).

---

**Total deviations:** 2 auto-fixed (both Rule 3 Blocking — both grep-criterion correctness). Both stem from the orchestrator's stricter zero-tolerance grep criteria vs. the plan's prose-level action text. Plan's verify block was always the binding contract; the action text was illustrative.

**Impact on plan:** Zero functional change. Plan's semantic intent preserved in every instance — in (1) the operator still learns what the supervisor is NOT, just without the banned acronym; in (2) every Azure reference maps to the equivalent Phase 5.1 pattern.

## Issues Encountered

None — this plan only touches markdown files so the usual executor-sandbox hazards (pnpm install, build, E2E) did not apply. `pnpm test` + `pnpm typecheck` both ran clean as the final verification step without intervention.

## User Setup Required

This plan's output IS the user setup material. Three operator artifacts must be executed BY the user (Claude cannot do them):

1. **Entra App Registration** — follow `docs/entra-app-registration-setup.md` end-to-end in the MMC Entra admin portal. ~20 minutes. Outputs: App Registration with client ID + tenant ID + client secret + App Role `KbAssistant.User` + pilot-group assignment + secrets written to AWS Secrets Manager.
2. **Windows Server provisioning** — follow `docs/deploy-windows.md` end-to-end on the Windows box. ~90 minutes for first run. Outputs: Node 20 installed, AWS credentials verified under the service account, deploy directories created, Windows Scheduled Task `KbAssistant` configured, IIS reverse proxy + ARR + URL Rewrite configured with SSE-safe settings, GitHub Actions self-hosted runner registered with `kbassistant` label.
3. **First deploy smoke** — push to `main` (or manually run the workflow). Watch the build → deploy → health canary sequence complete. Visit `https://<app-host>/` in a browser, sign in with a pilot user, ask a question, verify streaming tokens arrive incrementally.

See USER-SETUP.md (auto-generated from this plan's `user_setup:` frontmatter by the orchestrator) for the consolidated env-vars / dashboard-config / verification checklist across all three services.

## Next Phase Readiness

**Phase 5.1 is READY TO SHIP pending user execution of the two operator runbooks (`docs/entra-app-registration-setup.md` + `docs/deploy-windows.md`).**

- All 8 plans complete: 01 (deps/env/secrets) → 02 (msalClient + session) → 03 (auth routes) → 04 (middleware + chat + access-denied) → 05 (frontend BFF rewire) → 06 (dep+fixture surgical removal) → 07 (deploy workflow) → 08 (operator docs + ROADMAP — this plan).
- Code + tests: 597 unit tests green, 19 E2E specs + 2 skipped remote smoke, typecheck clean.
- Deploy infrastructure: `.github/workflows/deploy.yml` targets `[self-hosted, windows, kbassistant]` runner + Windows Scheduled Task + `/api/health` canary + `if: failure()` rollback.
- Operator docs: Entra App Registration walkthrough (203 lines) + Windows deploy walkthrough (339 lines) + env handling updated to reflect AWS Secrets Manager + loadSecrets() reality.
- ROADMAP.md: pivot note explains Phase 5 supersession; Phase 5.1 block has goal + 5 success criteria + 8-plan [x]-complete checklist; Requirements deferred to v1.1 annotations in place; Coverage table unchanged at 49/49.

**Blockers / concerns for downstream phases:**

- **Hard blocker for pilot launch:** user must execute `docs/entra-app-registration-setup.md` end-to-end in the MMC Entra admin portal. This is genuinely human-only (no CLI for App Role creation in most tenants). Until then, `/api/login` will fail with AADSTS errors.
- **Hard blocker for pilot launch:** user must execute `docs/deploy-windows.md` end-to-end on the Windows Server. Self-hosted runner must be registered with `kbassistant` label before `deploy.yml` can run; Scheduled Task must exist before the workflow's `schtasks /end` + `schtasks /run` calls have anything to act on.
- **Soft concern — v1.1 roadmap:** AUTH-03 (Teams SSO) + DELV-03 (Teams manifest) are annotated `(deferred to v1.1)` throughout the roadmap. A Phase 6.1 candidate for Teams-tab integration is mentioned in RESEARCH but is NOT on the v1 ROADMAP — left as a separate user-decision item per the orchestrator's "DO NOT add a Phase 6.1 entry without user confirmation" constraint. Revisit after pilot feedback.
- **Soft concern — Phase 6 prep:** observability + telemetry (Phase 6) will want to know about IIS access-log paths (`C:\inetpub\logs\LogFiles\W3SVC<n>\`), Node application log path (`D:\logs\kbassistant.log` if the PowerShell wrapper is used), Scheduled-Task event-log location (Event Viewer → Microsoft → Windows → TaskScheduler → Operational). All three are documented in `docs/deploy-windows.md` Step 7.2 Troubleshooting + the file/path inventory table — Phase 6 planning should pull log collection patterns from there rather than reinventing.

---
*Phase: 05.1-mmc-it-bff-pivot-xmcp-pattern*
*Completed: 2026-04-23*
